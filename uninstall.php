<?php 
/* 
 * Here you define what to do when the plugin is UNINSTALLED by the user, usually stuff like
 * removing custom options
 */

// If uninstall is not called from WordPress, exit
if ( !defined( 'WP_UNINSTALL_PLUGIN' ) ) exit();
